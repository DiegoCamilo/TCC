<!DOCTYPE html>
<html>
<head>
  <title>TCC</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<% if current_pessoa%>
		<%= link_to "Sair", destroy_pessoa_session_path %>
		<%= link_to current_pessoa.nome, edit_pessoa_registration_path(current_pessoa)%>
	<% end %>
	<p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
